It took me quite a while to sort through the many options for getting jasmine set up so I created this example repo.

Getting started
===============

$ brew install npm
$ npm install
$ grunt jasmine

